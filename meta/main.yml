galaxy_info:
  author: Arnaud Blancher
  description:  Take an ansible inventory and remove docker container for each listed hosts. This role is the companion of docker_provisionning
  license: GPLv3
  min_ansible_version: 2.0

  platforms:
  - name: Ubuntu
    versions:
    - precise
    - trusty
    - xenial
  - name: Debian
    versions:
    - jessie

  galaxy_tags:
    - docker
    - provision
    - provisionning
    - test
    - deploy

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
